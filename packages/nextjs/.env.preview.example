# Prisoners DAOlemma — Vercel preview env example
# Safe for public/browser-side configuration only.
# Do NOT place deployer/verifier private keys or keystore passwords here.

NEXT_PUBLIC_TARGET_NETWORK=baseSepolia
NEXT_PUBLIC_ALCHEMY_API_KEY=
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID=

# Optional later if artifacts move to external object storage:
# NEXT_PUBLIC_GAMES_DATA_BASE_URL=
